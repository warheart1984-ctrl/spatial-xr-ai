{
  "name": "spatial-xr-ai",
  "version": "0.2.0",
  "private": true,
  "description": "Spatial XR AI — Dual-Front intelligence control plane (Router v0, Equalizer artifacts, residual risk)",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "spatial-xr": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/*.test.js",
    "start": "node dist/cli.js",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^20.19.11",
    "typescript": "^5.9.2"
  }
}
